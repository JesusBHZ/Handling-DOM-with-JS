/*Atributos y propiedades 
atributos: type, placeholder, href (se utilizan al iniciar una etiqueta HTML)
propiedades: value, son modiificables (cambian segun su modificacion)
*/
const text = document.querySelector('input.btnn');
console.log(text);
console.log(text.value)
text.value = "Hola";